{
	"scripts": {
		"compile": "tsc",
		"rollup": "rollup --config rollup.config.js",
		"build": "npm run compile && npm run rollup"
	},
	"type": "module",
	"devDependencies": {
		"rollup": "^3.7.4",
		"typescript": "^4.9.4"
	}
}
